package com.adgent.trader.ui.widgets

import android.appwidget.AppWidgetProvider

/** Segnaposto F4 — widget Glance completi in fase F4. */
class TickerWidgetReceiver : AppWidgetProvider()

class WatchlistWidgetReceiver : AppWidgetProvider()
